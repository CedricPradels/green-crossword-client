import React, { useState } from "react";

import Default from "../../components/template/Default";

interface ICreate {}

const Edit: React.FC<ICreate> = () => {
  return <Default>test</Default>;
};

export default Edit;
